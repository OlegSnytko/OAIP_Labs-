#include<stdio.h>
#include<math.h>
int main()
{
	int userChoice;
	float circleArea, squareArea;
	printf("Choose the case: \n");
	printf("1 - a circle in a square \n");
    printf("2 - a square in circle \n");
	scanf_s("%d", &userChoice);

	while(scanf_s("%f %f", &circleArea, &squareArea) !=2);
	{
		printf("Error: input the number ");
		rewind(stdin);
	}
	printf("Input circle area, square area :");
	if (userChoice == 1)
	{
	  (2 * sqrt(circleArea / 3.14) < sqrt(squareArea));
		printf("Circle fits into a square");//Круг поместится.
	}
	else
	{
     printf("Circle doesn't fit the square ");
	}
	if (userChoice == 2)
	{
		(sqrt(2 * circleArea / 3.14) > sqrt(squareArea));
		printf("Square fits into a Circle");
	}
	else
	{
		printf("Square doesn't fit the Circle");
	}
	return 0;
}

	